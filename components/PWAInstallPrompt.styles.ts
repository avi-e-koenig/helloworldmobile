import { StyleSheet } from 'react-native';

export const createStyles = () =>
  StyleSheet.create({
    button: {
      marginBottom: 8,
    },
    card: {
      margin: 16,
    },
    dismissButton: {
      marginTop: 8,
    },
    icon: {
      marginBottom: 16,
      textAlign: 'center',
    },
    subtitle: {
      marginBottom: 8,
      textAlign: 'center',
    },
    surface: {
      alignItems: 'center',
      borderRadius: 8,
      marginBottom: 16,
      padding: 20,
    },
    title: {
      marginBottom: 8,
      textAlign: 'center',
    },
  });
